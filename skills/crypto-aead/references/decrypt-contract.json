{
  "contractVersion": "v1",
  "backend": "skill:crypto-aead.decrypt",
  "input": {
    "type": "object",
    "required": ["request"],
    "properties": {
      "request": {
        "type": "object",
        "required": ["algorithm", "key", "nonce", "ciphertext", "tag"],
        "properties": {
          "algorithm": {
            "type": "string",
            "enum": ["aes-256-gcm", "chacha20-poly1305"]
          },
          "key": {
            "type": "string",
            "description": "Hex-encoded encryption key"
          },
          "nonce": {
            "type": "string",
            "description": "Hex-encoded nonce"
          },
          "ciphertext": {
            "type": "string",
            "description": "Hex-encoded ciphertext"
          },
          "tag": {
            "type": "string",
            "description": "Hex-encoded authentication tag"
          },
          "aad": {
            "type": "string",
            "description": "Optional hex-encoded additional authenticated data"
          }
        }
      }
    }
  },
  "output": {
    "type": "object",
    "required": ["plaintext", "backend"],
    "properties": {
      "plaintext": {
        "type": "string",
        "description": "Hex-encoded plaintext"
      },
      "backend": {
        "type": "string",
        "const": "skill:crypto-aead.decrypt"
      }
    }
  }
}
