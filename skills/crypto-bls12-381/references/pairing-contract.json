{
  "contractVersion": "v1",
  "backend": "skill:crypto-bls12-381.pairing",
  "input": {
    "type": "object",
    "required": ["request"],
    "properties": {
      "request": {
        "type": "object",
        "required": ["operation", "points"],
        "properties": {
          "operation": {
            "type": "string",
            "enum": ["g1_add", "g2_add", "g1_mul", "g2_mul", "pairing"]
          },
          "points": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Array of hex-encoded points"
          },
          "scalar": {
            "type": "string",
            "description": "Optional hex-encoded scalar for multiplication operations"
          }
        }
      }
    }
  },
  "output": {
    "type": "object",
    "required": ["result", "operation", "backend"],
    "properties": {
      "result": {
        "type": "string",
        "description": "Hex-encoded result"
      },
      "operation": {
        "type": "string"
      },
      "backend": {
        "type": "string",
        "const": "skill:crypto-bls12-381.pairing"
      }
    }
  }
}
